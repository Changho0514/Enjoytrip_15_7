version: '3'

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"  # 프론트엔드 웹 서버 포트
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"  # 백엔드 서버 포트
